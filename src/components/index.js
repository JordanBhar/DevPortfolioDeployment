import Navbar from './Navbar';
import SkillsTab from './SkillsTab';
import EmailForum from './EmailForum';
import Footer from './Footer'

export {
    Navbar,
    SkillsTab,
    EmailForum,
    Footer
};

//file is used to compile all components into one place so only one reference (import) needs to be done 